# Terraform
.terraform/
.terraform.tfstate.lock.info
.terraformrc
terraform.rc

# Keep *.tfstate IN git (state stored in the repo for this project)
# but exclude backups and local plan files
*.tfstate.backup
*.tfstate.*backup*
tfplan
plan.txt
crash.log
crash.*.log

# Local variable overrides (put your SSH public key here locally)
*.tfvars
*.tfvars.json
*.auto.tfvars

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS / editor
.DS_Store
.idea/
*.swp
